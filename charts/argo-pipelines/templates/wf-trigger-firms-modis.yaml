{{- if .Values.workflows.triggerFirmsModis.enabled }}
# G12g-extension (2026-05-22) — firms-modis-eu (cron 20 */1).
# Pattern identique à trigger-usgs-earthquakes (cf
# argo_workflows_orchestrator_migration_pattern.md).
apiVersion: argoproj.io/v1alpha1
kind: CronWorkflow
metadata:
  name: trigger-firms-modis
  namespace: argo
spec:
  schedules:
    - "20 */1 * * *"
  concurrencyPolicy: Forbid
  successfulJobsHistoryLimit: 5
  failedJobsHistoryLimit: 5
  workflowSpec:
    entrypoint: main
    serviceAccountName: argo-workflow
    activeDeadlineSeconds: 300
    ttlStrategy:
      secondsAfterCompletion: 86400
    arguments:
      parameters:
        - name: api-url
          value: {{ .Values.workflows.triggerFirmsModis.apiUrl | quote }}
        - name: source-id
          value: {{ .Values.workflows.triggerFirmsModis.sourceId | quote }}
    templates:
      - name: main
        steps:
          - - name: trigger
              template: post-trigger
      - name: post-trigger
        retryStrategy:
          limit: 2
          backoff: { duration: "10s", factor: 2 }
        container:
          image: {{ .Values.workflows.triggerFirmsModis.image | quote }}
          command: [sh, -c]
          envFrom:
            - secretRef:
                name: {{ .Values.workflows.triggerFirmsModis.secretName | quote }}
          args:
            - |
              URL="{{api-url}}/internal-trigger/sources/{{source-id}}"
              curl -fsS -X POST -H "X-Service-Token: ${INTERNAL_TRIGGER_TOKEN}" "$URL"
          resources:
            requests: { cpu: "10m", memory: "16Mi" }
            limits:   { cpu: "100m", memory: "64Mi" }
{{- end }}
